




<div class="row">

      <% if(products.length > 0) { %>

      <% products.forEach((product) => { %>
      
            <div class="col-md-4">
                  <div class="card mb-4 b-shadow card-my-size" >
                        <img src="/img/<%= product.imageUrl %> " 
                              class="card-img-top" alt="<%= product.imageUrl %>">
                        <div class="card-body">
                              <h5 class="card-title">
                                    <a href="/products/<%= product.id %> "><%= product.name %></a>    
                                    </h5>
                        <p class="card-text">
                              <small style="font-weight: bolder;">
                                    <i class="fas fa-lira-sign"></i>  <%= product.price %> 
                              </small>   
                              
                        </p>
            
                        </div>
            
                        <div class="card-footer text-center">
                              
                              <a href="/products/<%= product.id %> " class="btn btn-dark btn-sm ">
                                    <i class="fas fa-angle-double-right"></i> Details
                              </a>

                              <form action="/cart" method="POST">
                                    <input type="hidden" name="productId" value="<%= product.id%>">
                                    <button  type="submit" class="btn btn-outline-success btn-sm" style="margin-left: 3px;">
                                          <i class="fas fa-cart-plus"></i>  Add to Cart
                                    </button>
                              </form>

                        </div>
                  </div>
            
            </div>
            
            <%     }) %>
      
      <%  }  else { %> 
            <div class="col-md-4 mt-4">
                  <h2>Görüntülenecek ürün yok</h2>
            </div>

      <%  } %>
</div>