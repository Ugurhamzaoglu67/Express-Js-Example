<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../layouts/head.ejs') %>
</head>

<body>

<%- include('../layouts/nav.ejs') %>

<div class="container pt-100">
      <div class="row mt-3">
            <div class="col-lg-3">
                 <%- include ('../layouts/leftmenu.ejs') %> 
            </div>

            <div class="col-lg-9">
                  <div class="row p-2" style='border: 1px solid black; border-radius: 5px;'>

                        <div class="col-md-4">
                              <img src="/img/<%= product.imageUrl %> " alt="<%= product.description %> " class="img-fluid">    
                        </div>

                        <div class="col-md-5 ">

                              <h2><%= product.name %> </h2>
                              <hr>

                              <p><%= product.description %> </p>
                              <p>Fiyat : <%= product.price %> <i class="fas fa-lira-sign"></i></p> 
                              <button type='submit' class="btn btn-success">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                              </button>
                        </div>

                  </div>
                  
            </div>
      </div>
</div>


<%- include('../layouts/footer.ejs') %>